@model IEnumerable<ClassicalMusicApp.Models.Thaat>

@{
    ViewBag.Title = "Thaat";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="page-header">Thaat</h2>

<table class="table">
    <tr>
        <th>
            Name
        </th>
        <th>
            Description
        </th>
        <th>
            Aroh
        </th>
        <th>
            Avroh
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(item.ThaatName, "Details", "Thaat", new { name = item.ThaatName.ToLower() }, null)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThaatDesc)
            </td>
            <td class="swara">
                @Html.DisplayFor(modelItem => item.Aroh)
            </td>
            <td class="swara">
                @Html.DisplayFor(modelItem => item.Avroh)
            </td>
        </tr>
    }

</table>
